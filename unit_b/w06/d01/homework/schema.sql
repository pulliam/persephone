CREATE TABLE blackbook (
	id INTEGER PRIMARY KEY,
	first_name VARCHAR(20),
	last_name VARCHAR(100),
	birth_year INTEGER,
	sex VARCHAR(6),
	p_number INTEGER,
	email VARCHAR (100),
	is_married BOOLEAN
	);